-- Extensões do Luma Global Trade Command são controladas pelo Prisma
-- através do bloco `extensions = [...]` em prisma/schema.prisma (feature
-- preview "postgresqlExtensions"). As extensões são criadas automaticamente
-- pela primeira migration gerada por `pnpm prisma:migrate`.
--
-- Este arquivo existe apenas como placeholder — não instala nada.
-- Se no futuro for necessário instalar algo que o Prisma não cobre
-- (ex.: extensões custom, dados de referência de bootstrap), adicionar aqui.

SELECT 1;
